# نظام إدارة الموارد البشرية (HR Management System)

نظام شامل لإدارة الموظفين والفروع في الشركة، مبني باستخدام Flask وSQLAlchemy.

## 📋 نظرة عامة

نظام إدارة الموارد البشرية يوفر واجهة سهلة الاستخدام لإدارة بيانات الموظفين والفروع، مع إمكانيات البحث المتقدم والتقارير التفصيلية.

## ✨ المميزات الرئيسية

### إدارة الموظفين
- ✅ إضافة وتعديل وحذف بيانات الموظفين
- ✅ تتبع البيانات الشخصية (الاسم بالعربية والإنجليزية، رقم البطاقة، الموبايل، البريد الإلكتروني)
- ✅ إدارة حالات الموظفين (نشط، تدريب، معلق، منتهي)
- ✅ تتبع المسمى الوظيفي (وكيل، قائد)
- ✅ إدارة بيانات الزي الموحد (قميص، بلوفر، بنطلون)
- ✅ تتبع استلام الفيزا وتاريخ الاستلام
- ✅ إدارة خط الديمو ورقمه
- ✅ تتبع تاريخ التعيين والاستقالة

### إدارة الفروع
- ✅ إضافة وتعديل وحذف بيانات الفروع
- ✅ إدارة معلومات الفروع (الاسم، الكود، رقم الهاتف، عنوان IP)
- ✅ إدارة بيانات الإدارة (المدير الإقليمي، المشرف، مدير الفرع)
- ✅ إدارة معلومات الموقع (الإحداثيات الجغرافية، العنوان)

### البحث والاستعلام
- ✅ بحث متقدم عن الموظفين
- ✅ البحث بالاسم (عربي/إنجليزي)
- ✅ البحث برقم البطاقة
- ✅ البحث باسم المستخدم
- ✅ تصفية حسب الفرع والحالة

### التقارير
- ✅ تقرير الموظفين النشطين
- ✅ تقرير الموظفين المنتهية خدمتهم
- ✅ تصدير التقارير بصيغة CSV
- ✅ تصفية التقارير حسب الفرع والشهر

### الأمان
- ✅ نظام مصادقة بكلمة مرور
- ✅ حماية العمليات الإدارية
- ✅ جلسات آمنة

## 🛠️ التقنيات المستخدمة

- **Backend Framework**: Flask 3.1.0
- **Database**: SQLite (قابل للتحويل إلى MySQL/PostgreSQL)
- **ORM**: SQLAlchemy 2.0.40
- **Template Engine**: Jinja2 3.1.6
- **Language**: Python 3.11+

## 📦 متطلبات التشغيل

- Python 3.11 أو أحدث
- pip (مدير الحزم)

## 🚀 التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd DataCastMangment
```

### 2. إنشاء بيئة افتراضية (Virtual Environment)

**على Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**على Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 4. تشغيل التطبيق

```bash
python src/main.py
```

أو:

```bash
flask run
```

سيتم تشغيل التطبيق على العنوان: `http://localhost:5000`

## ⚙️ الإعدادات والتكوين

### قاعدة البيانات

النظام يستخدم SQLite بشكل افتراضي. قاعدة البيانات يتم إنشاؤها تلقائياً في مجلد `instance/employee_system.db`.

للتغيير إلى MySQL أو PostgreSQL، قم بتعديل ملف `src/main.py`:

```python
# MySQL
app.config['SQLALCHEMY_DATABASE_URI'] = 'mysql+pymysql://user:password@localhost/dbname'

# PostgreSQL
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://user:password@localhost/dbname'
```

### كلمة المرور الإدارية

كلمة المرور الافتراضية للإدارة: `Admin123#`

يمكن تغييرها في ملف `src/auth.py`:

```python
ADMIN_PASSWORD = "YourPasswordHere"
```

## 📁 هيكل المشروع

```
DataCastMangment/
├── instance/
│   └── employee_system.db    # قاعدة البيانات
├── src/
│   ├── __init__.py
│   ├── main.py               # الملف الرئيسي للتطبيق
│   ├── db.py                 # إعدادات قاعدة البيانات
│   ├── auth.py               # نظام المصادقة
│   ├── models/               # نماذج البيانات
│   │   ├── employee.py      # نموذج الموظف
│   │   ├── branch.py        # نموذج الفرع
│   │   └── user.py          # نموذج المستخدم
│   ├── routes/               # مسارات التطبيق
│   │   ├── employee.py      # مسارات الموظفين
│   │   ├── branch.py        # مسارات الفروع
│   │   ├── search.py        # مسارات البحث
│   │   └── report.py        # مسارات التقارير
│   ├── templates/           # قوالب HTML
│   │   ├── index.html
│   │   ├── employees/
│   │   ├── branches/
│   │   ├── search/
│   │   ├── reports/
│   │   └── auth/
│   └── static/              # الملفات الثابتة
│       ├── css/
│       └── images/
├── venv/                    # البيئة الافتراضية
├── requirements.txt         # متطلبات المشروع
└── README.md               # ملف التوثيق
```

## 🔌 API Endpoints

### الموظفين

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/employees/` | عرض جميع الموظفين |
| GET | `/employees/api/list` | API لاسترجاع قائمة الموظفين |
| GET | `/employees/create` | عرض نموذج إضافة موظف (يتطلب مصادقة) |
| POST | `/employees/` | إنشاء موظف جديد (يتطلب مصادقة) |
| GET | `/employees/<id>` | عرض تفاصيل موظف |
| GET | `/employees/<id>/edit` | عرض نموذج تعديل موظف (يتطلب مصادقة) |
| PUT | `/employees/<id>` | تحديث بيانات موظف (يتطلب مصادقة) |
| DELETE | `/employees/<id>` | حذف موظف (يتطلب مصادقة) |

### الفروع

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/branches/` | عرض جميع الفروع |
| GET | `/branches/api/list` | API لاسترجاع قائمة الفروع |
| GET | `/branches/create` | عرض نموذج إضافة فرع (يتطلب مصادقة) |
| POST | `/branches/` | إنشاء فرع جديد (يتطلب مصادقة) |
| GET | `/branches/<id>` | عرض تفاصيل فرع |
| GET | `/branches/<id>/edit` | عرض نموذج تعديل فرع (يتطلب مصادقة) |
| PUT | `/branches/<id>` | تحديث بيانات فرع (يتطلب مصادقة) |
| DELETE | `/branches/<id>` | حذف فرع (يتطلب مصادقة) |

### البحث

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/search/` | عرض نموذج البحث |
| GET | `/search/results` | عرض نتائج البحث |
| GET | `/search/api` | API للبحث |

### التقارير

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/reports/` | عرض صفحة التقارير (يتطلب مصادقة) |
| GET | `/reports/api/active` | API للموظفين النشطين (يتطلب مصادقة) |
| GET | `/reports/api/terminated` | API للموظفين المنتهية خدمتهم (يتطلب مصادقة) |
| GET | `/reports/export/active` | تصدير تقرير الموظفين النشطين (يتطلب مصادقة) |
| GET | `/reports/export/terminated` | تصدير تقرير الموظفين المنتهية خدمتهم (يتطلب مصادقة) |

### المصادقة

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/logout` | تسجيل الخروج |

## 🗄️ هيكل قاعدة البيانات

### جدول الموظفين (employees)

| الحقل | النوع | الوصف |
|-------|------|-------|
| id | Integer | المعرف الفريد |
| branch_id | Integer | معرف الفرع |
| name_ar | String(255) | الاسم بالعربية |
| name_en | String(255) | الاسم بالإنجليزية |
| identification_number | String(50) | الرقم التعريفي |
| mobile_number | String(50) | رقم الموبايل |
| email | String(255) | البريد الإلكتروني |
| id_card_number | String(50) | رقم البطاقة |
| username | String(100) | اسم المستخدم |
| status | Enum | الحالة (Active, Training, Suspend, Termination) |
| title | Enum | المسمى (Agent, Leader) |
| received_shirt | Boolean | استلام القميص |
| received_sweater | Boolean | استلام البلوفر |
| received_pants | Boolean | استلام البنطلون |
| uniform_receipt_date | Date | تاريخ استلام الزي |
| received_visa | Boolean | استلم فيزا |
| visa_receipt_date | Date | تاريخ استلام الفيزا |
| received_demo_line | Boolean | استلم خط ديمو |
| demo_line_number | String(50) | رقم خط الديمو |
| hire_date | Date | تاريخ التعيين |
| resignation_date | Date | تاريخ الاستقالة |
| created_at | DateTime | تاريخ الإنشاء |
| updated_at | DateTime | تاريخ التحديث |

### جدول الفروع (branches)

| الحقل | النوع | الوصف |
|-------|------|-------|
| id | Integer | المعرف الفريد |
| name | String(255) | اسم الفرع |
| code | String(50) | كود الفرع (فريد) |
| phone | String(50) | رقم الهاتف |
| ip_address | String(50) | عنوان IP |
| regional_manager | String(255) | المدير الإقليمي |
| super_name | String(255) | اسم المشرف |
| area_name | String(255) | اسم المنطقة |
| area_email | String(255) | بريد المنطقة |
| branch_manager | String(255) | مدير الفرع |
| location_latitude | Float | خط العرض |
| location_longitude | Float | خط الطول |
| location_address | Text | العنوان |
| created_at | DateTime | تاريخ الإنشاء |
| updated_at | DateTime | تاريخ التحديث |

## 🔒 الأمان

- **المصادقة**: جميع العمليات الإدارية (إضافة، تعديل، حذف) تتطلب إدخال كلمة المرور
- **الجلسات**: استخدام جلسات آمنة مع مفتاح سري
- **SQL Injection**: استخدام SQLAlchemy ORM يحمي من هجمات SQL Injection
- **XSS Protection**: استخدام Jinja2 template engine مع escape تلقائي

## 📝 ملاحظات هامة

1. **النسخ الاحتياطي**: يرجى عمل نسخة احتياطية من قاعدة البيانات بانتظام
2. **كلمة المرور**: قم بتغيير كلمة المرور الافتراضية في بيئة الإنتاج
3. **قاعدة البيانات**: في بيئة الإنتاج، يُنصح باستخدام MySQL أو PostgreSQL بدلاً من SQLite
4. **الخادم**: للتشغيل على خادم إنتاج، استخدم خادم WSGI مثل Gunicorn أو uWSGI

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى:
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 👨‍💻 الدعم

للأسئلة أو المشاكل، يرجى فتح Issue في المستودع.

---

**تم التطوير بواسطة**: DataCast Management Team  
**الإصدار**: 1.0.0  
**آخر تحديث**: 2024

